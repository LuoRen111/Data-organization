**面试时间**：2026-04-09
**投递岗位**：Java开发
**面试轮次**：一面（笔试+技术负责人面试+机试）
**面试形式**：线下面试
**面试时长**：125分钟
**整体感受**：差 （有笔试不提前说明，提前声明我就不来了）

# 技术负责人面试

## Q0 : 当用户想要上传一个大文件（比如1GB）时，你的系统是如何接收它的？

A: 对于大文件，我采用的是‘分片上传 + 断点续传’的方式。我们会在前端先把大文件切成小的分片，比如 5MB 一块，然后并发地上传到后端。后端每收到一个分片，就存到 MinIO 中，同时会用 Redis 的 bitmap 去记录哪些分片已经上传成功。这样的好处就是，即使上传过程中断了，前端可以根据 Redis 状态判断哪些分片已经上传，不用从头开始，用户体验会比较好。

这里还有一个关键细节，就是首次上传分片时，我们会把这个文件的元信息，比如文件名、文件大小、上传者、所属组织标签等，保存到 MySQL 中，用来跟踪整个文件的上传状态。这也是为了方便后续的状态管理和权限控制。

当所有分片上传完成后，前端会调用后端的合并接口。这里我们用的是 MinIO 提供的 composeObject 功能，直接在存储端完成分片的合并，完全不占用服务器的内存和 CPU 资源。合并完成后，系统会把文件状态在 MySQL 里更新为‘已完成’，并且清理掉对应的分片文件和 Redis 记录。

最后，文件合并后我们还会发送一条 Kafka 消息，通知后台的异步服务去做后续的文件解析、文本切片、向量化等工作，保证上传接口本身是快速响应的，不会因为后端的耗时任务拖慢用户体验。

## Q1：Redis 的过期策略是怎么用的？

A：Redis 过期策略有**惰性过期**和**定期删除**两种。惰性过期是访问 key 时才判断是否过期，过期则删除；定期删除是 Redis 每隔一段时间随机抽查一批 key，检查并清除已过期键。面试时我一开始只记得定期删除，对惰性过期记忆模糊，经面试官提醒后才补充完整。

## Q2：Redis 常用的数据结构有哪些？

A：Redis 常用数据结构有**String、Hash、List、Set、ZSet**，跳表是 ZSet 的底层实现。面试时我以为他问的是底层数据结构 除了跳表，其他底层数据结构结构一时记不起来

## Q3：IoC 和 AOP 的原理？

A：IoC 是控制反转，由 Spring 容器负责对象创建与依赖注入，不用手动 new 对象，底层利用**反射机制**实现，降低代码耦合。AOP 是面向切面编程，把通用重复代码模块化提取，实现业务与通用逻辑解耦，支持前置、后置、环绕、异常抛出等通知类型。面试时回答 IoC 略有卡顿，AOP 表述有重复。

## Q4：实际使用过 AOP 吗？

A：实际使用过，最经典场景是**事务管理**，还可以用 AOP 统计接口 / 方法运行耗时、打印日志等通用操作。现场明确说明通知类型包含前置、后置、环绕、异常抛出，项目中确实用于事务与接口耗时统计。

## Q5：JPA 了解多少？

A：只听说过名字，没深入学，上手比较快。项目技术栈里虽有涉及，但我不清楚具体用在哪里。

(被问的时候我都懵圈了，不知道为啥问我这个问题)

## Q6：前端框架了解哪些？

A：了解 Vue，React 用过一点，开发过程都依赖 AI 工具辅助。

（我是纯后端）

## Q7：Vue 了解多少？

A：用过组件、组合式组件，能看懂代码，开发层面说不清原理，无法详细说明开发逻辑。

## Q8：什么原因离职？

A：虽然在上一家公司我学习到了很多的东西，但是上一家公司以驻场外包为主，我想打磨技术栈、不想做驻场，发展方向不匹配所以离职。

## Q9：对 AI 开发了解多少？

A：从 20202年 ChatGPT 到 DeepSeek，持续使用 Cursor、Cloud Code、Codex 等工具；了解 Mcp、tool caling、workflow、A2A、skills、；最近学了 Harness、Spring AI、langchain4j，还自学了机器学习与深度学习和transformer框架

## Q10：数据库用的是什么？版本？

A：数据库用 MySQL 和 Redis，MySQL 版本是 8.3，Redis 未明确说明版本。

## Q11：Java 用哪个版本？

A：Java 8 和 Java 21 都用过，ai 相关开发使用 JDK 21。

## Q12：期望薪资？

A：实习期 2500-3000 左右，转正后希望不低于 7k，越高越好，工作更安心。（7k是他给的最高薪资，再说高我怕他直接让我走了0_o)

## Q13：希望有交通补贴 / AI 会员吗？

A：地铁通勤 5 元，希望有交通补贴；希望公司开通 Cursor 或 Cloud Code、Codex 会员，我个人已买过部分 AI 会员。

## Q14：公司主要做什么业务？

A：主要做招标采购（招采）系统，属于定制化二次开发，类似 ERM 系统，曾给大学等机构做过项目。

## Q15：后续安排？

A：面试官要求我现场做一个简单开发任务，考察实际开发能力，我表示有时间配合完成。



# 笔试

![微信图片_20260408221800](C:\Users\罗任\Downloads\微信图片_20260408221800.jpg)

![微信图片_20260409002508](C:\Users\罗任\Downloads\微信图片_20260409002508.jpg)

![微信图片_20260408221759](C:\Users\罗任\Downloads\微信图片_20260408221759.jpg)



# 机试

我记得主要就是 使用springboot框架 来完成user表的增删改查与登陆注册 的前后端实现。

我就花了一个半小时完成了后端接口开发 通过apifox测试之后 就找借口跑路了。

# 总结

浪费我一个下午的时间 不要去小厂 ......

另外 写简历上面的内容八股 一定一定要背会背好背全。哪怕是涉及到的内容全都要背，因为你永远不知道面试官会针对你的简历问什么问题。